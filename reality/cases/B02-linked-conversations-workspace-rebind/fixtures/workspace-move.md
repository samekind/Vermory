# Thesis Workspace Move

The thesis workspace continuity starts at `/fixtures/thesis-old` and contains the governed fact that the final PDF is built with `make final-pdf`.

The repository is explicitly moved to `/fixtures/thesis-new`. Rebind must preserve the continuity ID and governed memory, retire the old path, and confirm the new path. Reversing the bridge must restore the old path and retire the new path.
