# Release Planning Conversation Excerpt

The release discussion reached two explicit decisions:

- Use `checkout_eta_v2` for the staged checkout release.
- Run the checkout smoke suite before increasing rollout percentage.

The same thread also contained an unconfirmed tangent about renaming the internal mascot. That tangent is not release authority and must not enter the software workspace.
