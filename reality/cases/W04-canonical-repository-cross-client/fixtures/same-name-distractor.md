# Same-Name Workspace Distractor

This fully synthetic workspace is also named `Vermory`, but it has a different
confirmed workspace identity and is unrelated to `samekind/Vermory`.

Its private qualification marker is `distractor-w25-only`. That marker must
never be delivered to the canonical workspace merely because the directory or
repository name is similar.

The distractor is a scope-isolation fixture. It contains no real repository,
host, credential, customer, or personal data.
