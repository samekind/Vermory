{
  "version": "1",
  "id": "W33-remote-git-workspace-topology",
  "reality_case_id": "W05-trusted-workspace-attachment",
  "tenant_id": "w33-remote-workspace-topology",
  "other_tenant_id": "w33-remote-workspace-topology-other",
  "filesystem_namespace": "device-alpha",
  "migrated_filesystem_namespace": "device-beta",
  "current_fact": "continuation_marker=w33-current",
  "other_tenant_fact": "continuation_marker=w33-other-tenant-only",
  "hard_gate_count": 24,
  "hard_gates": [
    "the mirror fixture is a real bare Git repository",
    "a bare mirror cannot be probed as a workspace attachment",
    "mirror fork and migrated checkouts share the primary commit graph",
    "mirror fork and migrated checkouts remain distinct attachments",
    "adding and changing remotes cannot change trusted attachment identity",
    "the confirmed primary still delivers its current governed fact after remote changes",
    "an unadopted mirror checkout abstains without a delivery",
    "an unadopted fork checkout abstains without a delivery",
    "an unbound cross-namespace migrated checkout abstains without a delivery",
    "the same migrated anchor in another tenant exposes only that tenant's fact",
    "explicit mirror adoption preserves the primary continuity",
    "an adopted mirror receives only current governed memory",
    "mirror adoption replay is idempotent",
    "mirror adoption reversal removes mirror access and preserves the primary",
    "explicit fork adoption preserves the primary continuity",
    "an adopted fork receives only current governed memory",
    "fork adoption reversal removes fork access",
    "explicit cross-namespace rebind preserves the primary continuity",
    "the migrated device receives only current governed memory",
    "cross-namespace rebind removes the old target-tenant binding",
    "the other tenant remains isolated after target-tenant rebind",
    "cross-namespace rebind replay is idempotent",
    "rebind reversal restores the exact source and target binding state",
    "a fresh final rebind serves the migrated device and leaves the old device unbound"
  ]
}
